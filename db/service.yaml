apiVersion: v1
kind: Service
metadata:
  name: payment-db
  namespace: fiap
  labels:
    app: payment-db
spec:
  selector:
    app: payment-db
  ports:
    - port: 27017
      targetPort: 27017
      name: payment-db
  type: ClusterIP